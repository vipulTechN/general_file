           <!-- data submitted successfully popup -->
            <div id="notificationPopup">Your data has been submitted successfully</div>
          <!-- data submitted successfully popup end -->

          <!-- data delete popup -->
            <div id="confirmationPopup">
              <p>Are you sure you want to delete?</p>
              <div class="confbutwrap">
              <button onclick="rejectDelete()">Cancel</button>
              <button onclick="confirmDeleteAction()">Delete</button>
              </div>
          </div>
          <!-- data delete popup end-->

          <!-- Delete confirmation popup -->
          <div id="deleteConfirmationPopup">
              <p>Your data has been deleted successfully.</p>
          </div>
          <!-- Delete confirmation popup end -->
          
          <!-- overlay for background -->
            <div id="overlay"></div>
          <!-- overlay for background end-->


   #notificationPopup,#confirmationPopup,#deleteConfirmationPopup {
      display: none;
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      background-color: #fff;
      padding: 10px 20px;
      border: 1px solid #ccc;
      border-radius: 5px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
      z-index: 200
    }

    #notificationPopup p,#confirmationPopup p{
      font-weight: 600;
      font-size: 16px;
      text-align: center;
    }

    #overlay {
      display: none;
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 0, 0, 0.5);
      z-index: 100;
    }

    .confbutwrap{
      width: 100%;
      display: flex;
      justify-content: space-evenly;
      align-items: center;
    }

    .confbutwrap button{
      border-radius: 2px;
      border: none;
      font-size: 12px;
      color: white;
      padding: 4px 10px;
      font-weight: 600;
    }

    .confbutwrap button:nth-child(1){
      background-color: green;
    }

    .confbutwrap button:nth-child(2){
      background-color: red;
    }


    @media only screen and (max-width:1170px) {
      .table-container,
      .newsec {
        width: 100%;
        max-width: 100%;
      }
    }


<script>
    function saveData() {
      var notificationPopup = document.getElementById('notificationPopup');
      var overlay = document.getElementById('overlay');
      overlay.style.display = 'block';
      notificationPopup.style.display = 'block';
      setTimeout(function() {
        notificationPopup.style.display = 'none';
        overlay.style.display = 'none';
      }, 2000);
    }
  </script>
  <script>
    function confirmDelete() {
      var confirmationPopup = document.getElementById('confirmationPopup');
      var overlay = document.getElementById('overlay');
      overlay.style.display = 'block';
      confirmationPopup.style.display = 'block';
    }

    function rejectDelete() {
      var confirmationPopup = document.getElementById('confirmationPopup');
      var overlay = document.getElementById('overlay');
      confirmationPopup.style.display = 'none';
      overlay.style.display = 'none';
    }

    function confirmDeleteAction() {
      var confirmationPopup = document.getElementById('confirmationPopup');
      var overlay = document.getElementById('overlay');
      confirmationPopup.style.display = 'none';
      overlay.style.display = 'none';

      var deleteConfirmationPopup = document.getElementById('deleteConfirmationPopup');
      deleteConfirmationPopup.style.display = 'block';
      setTimeout(function() {
        deleteConfirmationPopup.style.display = 'none';
      }, 2000);
    }
  </script>





line no 266 in style.css change this css with old
tbody tr td {
    font-weight: 500;
    font-size: 15px !important;
    border: 1px solid rgba(0, 0, 0, .1);
    border-bottom: 1px solid rgba(0, 0, 0, .599);
    text-align: center !important;
}